spring:
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
  datasource:
    url:  jdbc:mysql://localhost:3306/mybatis?useUnicode=true&characterEncoding=UTF-8&&serverTimezone=Asia/Shanghai&allowMultiQueries=true&autoReconnect=true
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root1234
